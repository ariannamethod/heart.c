[slot]
id = "heart/doe"
description = "phone-1 heart.c — DoE voice turn (Janus 170M + Parliament SFT)"
exec = "heart converse --voice doe --max_tokens 64"
async = false
args_schema = "[{name:'prompt',type:'string',optional:true}]"
output = "stdout text (turn transcript)"

[requires]
ram = "500MB"
disk = "0"
gpu = false

[provides]
streaming_metrics = ""

[ownership]
arch = ["aarch64"]
os   = ["android"]
